# 总体目标
诊断并修复 srsRAN_4G eNB 的 MAC 层资源分配错误。
**核心错误特征**：`SCHED: Could not allocate SI message... Cause: Effective coderate exceeds threshold`。

# 执行规则 (Strict Rules for Agent)
1. 严格按照以下 Phase 和步骤顺序执行，不得跳过。
2. 每次修改代码后，必须记录修改位置。
3. 遇到达到上限的重试次数（如编译失败 5 次，或探查失败 5 次），必须立即停止并使用 `ask_user` 工具或直接在聊天中请求用户介入，绝对禁止无限循环。
4. 只对srsRAN_4G的源代码的修改才可以进行写入操作，不要进行任何其他的写入操作（也不要安装任何工具）
5. 编译、运行 EPC/eNB、停止进程都已经是shell cripts，并且经过验证是可以运行，不要应用系统级的build工具如cmake，make等。
---

## 🟢 Phase 1: 环境上下文加载
- [ ] 1. 读取memory_bank目录下的文件，理解项目的总体情况。
- [ ] 2. 在 `activeContext.md` 文件中提取并理解在 `nuc5GC` 上执行编译、运行 EPC/eNB、停止进程以及读取日志的具体命令和路径。
- [ ] 3. 向用户简要汇报（/smol）你提取到的关键路径和命令，确认无误后进入下一阶段。

## 🟡 Phase 2: 探查与代码插桩 (循环起点，最大允许循环: 5次)
**当前探查循环次数：0/5**
- [ ] 1. **代码静态分析**：在 `nuc5GC` 的 srsRAN_4G 源码目录中，使用 `grep` 或其他搜索工具，定位处理 SIB（System Information Block）调度和发射的 MAC 层函数（重点关注包含 "Could not allocate SI message" 或 "Effective coderate exceeds threshold" 的文件，通常在 `srsenb/src/mac/` 下）。
- [ ] 2. **代码修改 (插桩)**：在目标函数的关键逻辑路径（如计算 coderate、分配 PRB 资源、读取阈值的判断条件前后），注入额外的 log 打印输出代码（使用 srsRAN 原生的 `srslog` 或 `Info`/`Debug` 宏，或标准输出），以便打印出当前的实际变量值（如当前分配的 PRB 数量、计算出的有效码率值、预设的阈值等）。

## 🟠 Phase 3: 编译与运行验证
- [ ] 1. **执行编译**：在 `activeContext.md` 文件中有具体说明。
- [ ] 2. **编译状态检查 (最大重试: 5次)**：
    - 读取 `make_tpm.log` 的最后几十行。
    - 如果编译成功，继续下一步。
    - 如果编译失败，分析错误，修改代码修复语法/逻辑错误，返回本阶段第 1 步重新编译。如果累计失败达到 5 次，**立刻停止并请求用户介入**。
- [ ] 3. **运行测试**：
    - 启动 EPC 服务（后台）。
    - 启动 eNB 服务（后台）。
    - 等待/休眠 (sleep) 30 秒。
    - 优雅地停止 eNB 和 EPC 进程（使用在 Phase 1 学到的方法）。

## 🔴 Phase 4: 日志分析与根因判定
- [ ] 1. **读取日志**：抓取 eNB 生成的日志文件，过滤出我们刚才在 Phase 2 注入的自定义 log 输出，以及原始的 `SCHED: Could not allocate SI message` 报错。
- [ ] 2. **根因推断**：
    - 打印出的实际码率是多少？阈值是多少？
    - 是因为配置的下行带宽（PRB）太小？还是 SIB 载荷（Payload）过大？或者是 MCS 配置不合理？
- [ ] 3. **循环判定**：
    - 如果依据当前的 log 已经**完全确定**了引发这个错误的代码逻辑或参数根因，进入 Phase 5。
    - 如果信息仍不足以定位根本原因，且当前 Phase 2 的循环次数小于 5 次，请重新构思需要打印的变量，返回 **Phase 2** 再次注入新的 log。
    - 如果已经循环了 5 次仍未查明，向用户提交一份当前排查进展报告，并请求人工介入。

## 🟢 Phase 5: 错误修复与最终验证
- [ ] 1. **制定修复计划**：在聊天窗口中输出一段简短的修复思路（例如：修改默认 MCS、放宽判定阈值、或修复错误的 PRB 计算公式）。
- [ ] 2. **执行代码修复**：根据计划修改 `nuc5GC` 上的源代码。
- [ ] 3. **最终编译**：执行编译（同 Phase 3，重定向 log）。
- [ ] 4. **最终运行**：启动 EPC 和 eNB，运行 30 秒后停止。
- [ ] 5. **最终验证**：分析最新日志，确认 `SCHED: Could not allocate SI message` 错误是否已经消失，且系统信息调度是否恢复正常。
- [ ] 6. **输出结案报告**：向用户总结本次修复修改了哪些文件、哪个函数、具体逻辑是什么，以及最终的验证结果。任务结束。



